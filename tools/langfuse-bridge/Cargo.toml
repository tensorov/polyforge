[package]
name = "langfuse-bridge"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Optional analytics bridge that posts PolyForge gate scores to a self-hosted Langfuse instance. Never part of the trust contour."
license = "Apache-2.0"
publish = false

[dependencies]
serde_json = "1"

# Standalone on purpose: NOT a member of the PolyForge workspace or trust contour.
[workspace]
